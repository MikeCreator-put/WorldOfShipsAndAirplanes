package enums;

/**
 * Includes various states in which a <i>Ship<i/>ship can be at the moment of time.
 * Each one of those states will be handled differently.
 */
public enum ShipStatus {
    travelling,
    arrived,
    waiting,
}
